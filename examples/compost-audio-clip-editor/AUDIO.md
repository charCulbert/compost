# Demo audio

`seq1-excerpt.wav` is the first 10 seconds of `seq1.mp3`, recorded by Charlie
Culbert and included with his permission. Converted to stereo 48 kHz, 16-bit
PCM WAV without normalization. The example's four-bar grid is illustrative;
it does not represent detected tempo or beat alignment.
